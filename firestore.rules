rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /devices/{docId} {
      allow read, write: if false; // only the Admin SDK (Cloud Functions) touches this
    }
  }
}
